import 'package:flutter/material.dart';

class AppColors {
  static const appBarBackground = primary;
  static const appBarBackgroundDark = Color(0xFF202c33);
  static const appBarIcon = Color(0xFFffffff);
  static const appBarIconDark = Color(0xFF8696a0);

  static const scaffoldBackground = Color(0xFFffffff);
  static const scaffoldBackgroundDark = Color(0xFF111b21);

  static const primary = Color(0xFF008069);
  static const primaryLight = Color(0xFF25d366);
  static const primaryDark = Color(0xFF00533e);

  static const primaryIconLight = Color(0xFF25d366);
  static const primaryIconDark = Color(0xFF00a884);

  static const icon = Color(0xFF8696a0);
  static const iconDark = Color(0xFF8696a0);

  // static const dividerColor = Color(3);
  // static const dividerColorDark = Color(3);

  // text color
  static const defaultTitle = Color(0xFF111b21);
  static const defaultTitleDark = Color(0xFFe9edef);

  static const defaultCaption = Color(0xFF6a7a84);
  static const defaultCaptionDark = Color(0xFF888d90);
}
